import { Container, Title } from "@mantine/core";

const Settings = () => {
  return (
    <Container size="md" py="xl">
      <Title order={1} mb="xl">
        Settings
      </Title>
    </Container>
  );
};

export default Settings;
